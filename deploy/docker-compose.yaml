version: '3'
services:
  hadar:
    image: docker.pkg.github.com/hadar-simulator/simple-server/server:0.3.1
    environment:
      GARBAGE_LOOP_SEC: 30
      # ACCESS_TOKEN: 'password'
      DB_PATH: '/data/db.sqlite3'
    ports:
    - '8080:8080'
    volumes:
    - '/home/ubuntu/data:/data'